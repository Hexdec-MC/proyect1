model Supplier {
  id        Int      @id @default(autoincrement())
  name      String
  contact   String?
  email     String?
  phone     String?
  createdAt DateTime @default(now())
  purchases Purchase[]
}
